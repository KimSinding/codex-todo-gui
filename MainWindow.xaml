<Window x:Class="TodoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Todo List" Width="500" Height="600" MinHeight="280" MinWidth="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Button DockPanel.Dock="Right" Width="80" Margin="8,0,0,0" Click="AddButton_Click">Add</Button>
            <TextBox x:Name="NewTaskTextBox" VerticalContentAlignment="Center" KeyDown="NewTaskTextBox_KeyDown" />
        </DockPanel>

        <ListBox x:Name="TasksListBox" Grid.Row="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Text}"
                              IsChecked="{Binding IsDone}"
                              Checked="TaskCheckBox_Changed"
                              Unchecked="TaskCheckBox_Changed"
                              Padding="4" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="4" Width="100" HorizontalAlignment="Right" Click="DoneButton_Click">Done</Button>
    </Grid>
</Window>
